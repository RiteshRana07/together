"use client";
import {useCallback,useEffect,useRef,useState} from "react";
const ICE_SERVERS=[{urls:"stun:stun.l.google.com:19302"},...(process.env.NEXT_PUBLIC_TURN_URL?[{urls:process.env.NEXT_PUBLIC_TURN_URL,username:process.env.NEXT_PUBLIC_TURN_USERNAME,credential:process.env.NEXT_PUBLIC_TURN_CREDENTIAL}]:[])];
export default function CallPanel({channel,broadcast,userId,username,participants,roomCode}){const pcs=useRef(new Map()),streams=useRef(new Map()),pending=useRef(new Map()),localRef=useRef(null),uid=useRef(userId),parts=useRef(participants),sendRef=useRef(null),signalSince=useRef(0),seen=useRef(new Set());const[joined,setJoined]=useState(false),[mic,setMic]=useState(false),[camera,setCamera]=useState(false),[localStream,setLocalStream]=useState(null),[callError,setCallError]=useState("");const[,force]=useState(0);useEffect(()=>{uid.current=userId},[userId]);useEffect(()=>{parts.current=participants||[]},[participants]);const closePeer=useCallback(id=>{try{pcs.current.get(id)?.close()}catch{}pcs.current.delete(id);streams.current.delete(id);pending.current.delete(id);force(n=>n+1)},[]);const sendSignal=useCallback(async(to,payload)=>{if(!roomCode)return;try{await fetch(`/api/rooms/${roomCode}/signals`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({to,...payload})})}catch(e){console.warn("[call] signal send failed",e)}},[roomCode]);useEffect(()=>{sendRef.current=sendSignal},[sendSignal]);const getPeer=useCallback(id=>{if(pcs.current.has(id))return pcs.current.get(id);const pc=new RTCPeerConnection({iceServers:ICE_SERVERS});const stream=localRef.current;if(stream)stream.getTracks().forEach(t=>pc.addTrack(t,stream));pc.onicecandidate=e=>{if(e.candidate)sendRef.current?.(id,{candidate:e.candidate})};pc.ontrack=e=>{const s=e.streams?.[0];if(s){streams.current.set(id,s);force(n=>n+1)}};pc.onconnectionstatechange=()=>{if(["failed","closed"].includes(pc.connectionState))closePeer(id);else if(pc.connectionState==="disconnected")setTimeout(()=>{const x=pcs.current.get(id);if(x?.connectionState==="disconnected")closePeer(id)},4000)};pcs.current.set(id,pc);return pc},[closePeer]);const flush=useCallback(async(id,pc)=>{const list=pending.current.get(id)||[];pending.current.delete(id);for(const c of list){try{await pc.addIceCandidate(c)}catch{}}},[]);const offer=useCallback(async id=>{if(!localRef.current||!joined||String(id)===String(uid.current)||String(uid.current)>String(id)||pcs.current.has(id))return;const pc=getPeer(id);if(pc.signalingState!=="stable")return;try{const o=await pc.createOffer({offerToReceiveAudio:true,offerToReceiveVideo:true});await pc.setLocalDescription(o);await sendRef.current?.(id,{description:pc.localDescription})}catch(e){console.warn("[call] offer failed",e)}},[getPeer,joined]);const handleSignal=useCallback(async m=>{if(!joined||!m||String(m.to)!==String(uid.current)||String(m.from)===String(uid.current))return;if(m.id&&seen.current.has(m.id))return;if(m.id)seen.current.add(m.id);const id=String(m.from),pc=getPeer(id);if(m.description){try{if(m.description.type==="offer"&&pc.signalingState!=="stable"){if(pc.signalingState==="have-local-offer")await pc.setLocalDescription({type:"rollback"});else{closePeer(id);return}}await pc.setRemoteDescription(m.description);await flush(id,pc);if(m.description.type==="offer"){const a=await pc.createAnswer();await pc.setLocalDescription(a);await sendRef.current?.(id,{description:pc.localDescription})}}catch(e){console.warn("[call] description failed",e);closePeer(id)}}if(m.candidate){try{if(pc.remoteDescription)await pc.addIceCandidate(m.candidate);else{const list=pending.current.get(id)||[];list.push(m.candidate);pending.current.set(id,list)}}catch{}}},[joined,getPeer,closePeer,flush]);useEffect(()=>{if(!joined||!roomCode)return;const poll=async()=>{try{const r=await fetch(`/api/rooms/${roomCode}/signals?since=${signalSince.current}`,{cache:"no-store"});if(!r.ok)return;const d=await r.json();for(const s of d.signals||[]){signalSince.current=Math.max(signalSince.current,Number(s.at)||0);await handleSignal(s)}}catch(e){console.warn("[call] signal poll failed",e)}};poll();const t=setInterval(poll,500);return()=>clearInterval(t)},[joined,roomCode,handleSignal]);useEffect(()=>{if(!joined)return;parts.current.filter(p=>String(p.id)!==String(uid.current)).forEach(p=>offer(String(p.id)))},[joined,participants,offer]);async function toggleCall(){if(joined){pcs.current.forEach(pc=>{try{pc.close()}catch{}});pcs.current.clear();streams.current.clear();pending.current.clear();localRef.current?.getTracks().forEach(t=>t.stop());localRef.current=null;setLocalStream(null);setJoined(false);setMic(false);setCamera(false);setCallError("");force(n=>n+1);return}try{setCallError("");const s=await navigator.mediaDevices.getUserMedia({audio:true,video:true});s.getAudioTracks().forEach(t=>t.enabled=false);s.getVideoTracks().forEach(t=>t.enabled=false);localRef.current=s;setLocalStream(s);setJoined(true)}catch(e){console.error("[call] getUserMedia",e);setCallError("Camera and microphone permission is required for room calls.")}}function toggle(kind){const s=localRef.current;if(!s)return;const tracks=kind==="mic"?s.getAudioTracks():s.getVideoTracks();const next=kind==="mic"?!mic:!camera;tracks.forEach(t=>t.enabled=next);kind==="mic"?setMic(next):setCamera(next)}useEffect(()=>()=>{pcs.current.forEach(pc=>{try{pc.close()}catch{}});localRef.current?.getTracks().forEach(t=>t.stop())},[]);const rem=Array.from(streams.current.entries());return <div className="space-y-4"><div className="flex items-center justify-between"><div><p className="eyebrow">LIVE CALL</p><h3 className="text-lg font-semibold mt-1">Talk while you watch</h3></div><span className={`status-pill ${joined?"status-live":""}`}>{joined?"Call connected":"Not in call"}</span></div><p className="text-sm leading-6 text-white/50">Start muted and camera-off. Turn either on when you are ready.</p>{callError&&<p className="text-xs text-red-300">{callError}</p>}{joined&&<div className="grid grid-cols-2 gap-3"><div className="call-tile"><video ref={el=>{if(el&&localStream)el.srcObject=localStream}} autoPlay muted playsInline/><div className="call-name">{username} <span>(You)</span></div></div>{rem.map(([id,stream])=><div key={id} className="call-tile"><video ref={el=>{if(el)el.srcObject=stream}} autoPlay playsInline/><div className="call-name">{participants.find(p=>String(p.id)===String(id))?.username||"Guest"}</div></div>)}</div>}<div className="flex gap-2 flex-wrap"><button onClick={()=>toggle("mic")} disabled={!joined} className="wt-button wt-button-ghost">{mic?"Mic on":"Mic off"}</button><button onClick={()=>toggle("camera")} disabled={!joined} className="wt-button wt-button-ghost">{camera?"Camera on":"Camera off"}</button><button onClick={toggleCall} className={`wt-button ${joined?"wt-button-danger":"wt-button-primary"}`}>{joined?"Leave call":"Join call"}</button></div></div>;}
